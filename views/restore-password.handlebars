<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Olvidaste tu contraseña</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-image: url('https://p4.wallpaperbetter.com/wallpaper/561/441/224/minimalistic-dark-grey-textures-gradient-2000x1200-abstract-textures-hd-art-wallpaper-preview.jpg');
        background-size: cover;
        background-repeat: no-repeat;
        background-position: center;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        margin: 0;
      }

      .containerAuth {
        max-width: 520px;
        width: 100%;
        padding: 20px;
        background-color: rgba(255, 255, 255, 0.92);
        border: 1px solid #ddd;
        border-radius: 10px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.15);
      }

      .btn-dark {
        background-color: #000;
        border: none;
      }
      .btn-dark:hover {
        background-color: #111;
      }

      .muted {
        color: #666;
        font-size: 0.95rem;
      }
    </style>
  </head>
  <body>
    <div class="containerAuth">
      <h1 class="h3 text-center mb-2">¿Olvidaste tu contraseña?</h1>
      <p class="muted text-center mb-4">
        Te mandamos un <b>código</b> a tu email para que puedas crear una nueva.
        Si no lo ves, revisá <b>Spam / Promociones</b>.
      </p>

      <div class="card mb-3">
        <div class="card-body">
          <h2 class="h6 mb-3">1) Pedir código</h2>
          <form id="formRequestCode" autocomplete="off">
            <div class="mb-3">
              <label class="form-label" for="email">Email</label>
              <input
                id="email"
                name="email"
                type="email"
                class="form-control"
                placeholder="tuemail@dominio.com"
                required
              />
            </div>
            <button type="submit" class="btn btn-dark w-100">Enviar código</button>
          </form>
        </div>
      </div>

      <div class="card">
        <div class="card-body">
          <h2 class="h6 mb-3">2) Cambiar contraseña</h2>
          <form id="formResetPassword" autocomplete="off">
            <div class="mb-3">
              <label class="form-label" for="code">Código</label>
              <input
                id="code"
                name="code"
                type="text"
                class="form-control"
                placeholder="Pegá el código que te llegó"
                required
              />
            </div>
            <div class="mb-3">
              <label class="form-label" for="newPassword">Nueva contraseña</label>
              <input
                id="newPassword"
                name="newPassword"
                type="password"
                class="form-control"
                placeholder="Nueva contraseña"
                required
              />
            </div>
            <button type="submit" class="btn btn-dark w-100">Guardar nueva contraseña</button>
          </form>
        </div>
      </div>

      <div class="d-flex justify-content-between align-items-center mt-3">
        <a href="/auth/login" class="text-decoration-none">← Volver a iniciar sesión</a>
        <a href="https://wa.me/+541162366175" target="_blank" class="text-decoration-none">¿Necesitás ayuda?</a>
      </div>
    </div>

    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/restorePassword.js"></script>
  </body>
</html>
